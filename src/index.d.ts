import { Common } from './authorizenet.common';
export declare class Authorizenet extends Common {

    constructor(props: object)

    fetchToken(options: Object) : Promise<any>

    createCardNumber(credentials: Object) : any

}
